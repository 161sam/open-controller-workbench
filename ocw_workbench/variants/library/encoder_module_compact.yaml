variant:
  id: "encoder_module_compact"
  name: "Encoder Module Compact"
  description: "Compact four-encoder variant with tighter spacing"
  template_id: "encoder_module"
  category: "module"
  tags:
    - "encoder"
    - "compact"
  version: "1.0"

overrides:
  controller:
    surface:
      width: 104
  zones:
    - id: "main_controls"
      type: "grid"
      x: 10
      y: 10
      width: 84
      height: 40
      strategy: "grid"
  layout:
    config:
      spacing_x_mm: 20
      spacing_y_mm: 18
  defaults:
    handedness: "center"
